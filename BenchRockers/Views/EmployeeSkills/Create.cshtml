@model BenchRockers.Models.EmployeeSkills

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>EmployeeSkills</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.EmpId, "Employee")
        </div>
        <div class="editor-field">
            @Html.DropDownList("EmpId", String.Empty)
            @Html.ValidationMessageFor(model => model.EmpId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SkillId, "Skills")
        </div>
        <div class="editor-field">
            @Html.DropDownList("SkillId", String.Empty)
            @Html.ValidationMessageFor(model => model.SkillId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Rating)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Rating)
            @Html.ValidationMessageFor(model => model.Rating)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
